# No environment variables are required to build, run, or deploy this app.
#
# RISE RPC URLs are non-sensitive and configured in src/config/deployments.ts.
# Market reads, oracle/access checks, and shadow preflight run client-side via those RPCs;
# execution happens in the connected wallet. There is no backend, database, or auth secret.
